@using DoAn_Auction.Models;
@model List<Category>
@{
    ViewBag.Title = "Add";
    Layout = "~/Views/Shared/_Admin.cshtml";
}

<div class="row">
    <ol class="breadcrumb">
        <li><a href="#"><svg class="glyph stroked home"><use xlink:href="#stroked-home"></use></svg></a></li>
        <li class="active">Danh mục</li>
    </ol>
</div><!--/.row-->

<div class="row">
    <div class="col-lg-12">
        <h1 class="page-header">Danh mục</h1>
        <div class="col-md-6">
            <form method="post">
                <div class="form-group">
                    <label>Danh mục cha</label>
                    <select class="form-control" name="ParentID">
                        @foreach (var cpr in Model)
                        {
                            <option value="@cpr.CatID">@cpr.CatName</option>
                        }
                    </select>
                </div>
                <div class="form-group">
                    <label>Tên danh mục</label>
                    <input type="text" class="form-control" placeholder="Tên danh mục" name="CatName">
                </div>
                <button type="submit" class="btn btn-primary"><i class="glyphicon glyphicon-check"></i> Thêm</button>
            </form>
        </div>
        </div>
</div><!--/.row-->
