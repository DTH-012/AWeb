@using DoAn_Auction.Models
@model List<Category>
<div class="col-sm-8 col-md-6 search-box m-t-2">
    <form class="input-group" action="@Url.Action("Result", "Search")">
        <input type="text" class="form-control" aria-label="Search here..." placeholder="Tìm kiếm..." name="q" value="@ViewBag.q">
        <div class="input-group-btn">
            <select class="selectpicker hidden-xs" data-width="150px" name="cat">
                <option value="0">Danh mục</option>
                @foreach (var cat in Model)
                {
                    <optgroup label="@cat.CatName">
                        @foreach (var cat_child in cat.Categories)
                        {
                            <option value="@cat_child.CatID">@cat_child.CatName</option>
                        }
                    </optgroup>
                }
            </select>
            <button type="submit" class="btn btn-default btn-search"><i class="fa fa-search"></i></button>
        </div>
    </form>
</div>