@using DoAn_Auction.Helpers;
<div class="account-picture"><img src="~/Images/demo/user.png" alt="" class="img-circle img-responsive"></div>
<h4 class="text-center m-b-3">
    @{
        string eName = null;
       if(CurrentContext.IsLogged()==true)
        {
            eName = CurrentContext.GetCurUser().f_Name;
        }
    }
    @eName
</h4>
<ul class="nav nav-pills nav-stacked">
    <li role="presentation" class="@(ViewBag.Current == "Profile" ? "active" : "")"><a href="@Url.Action("Profile","Account")">Thông tin tài khoản</a></li>
    <li role="presentation" class="@(ViewBag.Current == "ChangePassword" ? "active" : "")"><a href="@Url.Action("ChangePassword","Account")">Đổi mật khẩu</a></li>
    <li role="presentation" class="@(ViewBag.Current == "ReviewDetail" ? "active" : "")"><a href="@Url.Action("ReviewDetail","Account")">Điểm đánh giá</a></li>
    <li role="presentation" class="@(ViewBag.Current == "Favorite" ? "active" : "")"><a href="@Url.Action("Favorite","Account")">Sản phẩm yêu thích</a></li>
    <li role="presentation" class="@(ViewBag.Current == "Bidding" ? "active" : "")"><a href="@Url.Action("Bidding","Account")">Sản phẩm đang đấu giá</a></li>
    <li role="presentation" class="@(ViewBag.Current == "BidWin" ? "active" : "")"><a href="@Url.Action("BidWin","Account")">Sản phẩm đã thắng</a></li>
    <li role="presentation" class="@(ViewBag.Current == "YourProduct" ? "active" : "")"><a href="@Url.Action("YourProduct", "Account")">Sản phẩm đang được đăng bán</a></li>
    <li role="presentation" class="@(ViewBag.Current == "YourCustomer" ? "active" : "")"><a href="@Url.Action("YourCustomer", "Account")">Sản phẩm đã có người mua</a></li>
</ul>
